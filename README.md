# selectel
---

## Запуск линтера

```text
go run cmd/linter/main.go ./...
```
---

## Запуск тестов

```text
go run cmd/linter/main.go ./...
```
---

## Примечание
```text
для заглвной буквы и спец. символов есть авто исправление командой 
go run cmd/linter/main.go --fix ./...


P.S. создать плагин мне так и не удалось я пытался и module и binary но потратив сутки я так и не сумел( 
максимум я смог сделать модульный плагин, то есть сбилдить новый бинарь в который вшит мой анализтор, его название logcheck
и находится он в корне репозитория, но его проблема в том что он привязан к go module, который так же находится в репозитории
и при попытке перенести бинарь в GOPATH, анализатор перестает работать и превращается в обычный бинарник golangci lint
```